[H,Q] = alist2sparse('test.alist') %parity check matrix in sparse format
[M,N] = size(H) % defining sizes
m  = 9;  % gf(2^m)
p = 0.9
q = 256
out = randsrc(1,100,[0,randi([1 q-1]);p,1-p]) %received sequince
theta = 0; %threshold
%alg1(r, m, H, theta) % algorithm 1